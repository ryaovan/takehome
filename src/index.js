import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

/* Create functionality in a sensible file to do the following:
 * TODO - retrieve the floor plan from the provided studio.json
 * TODO - retrieve floor info from the provided floors.json
 * TODO - display them on the page per requirements
 */